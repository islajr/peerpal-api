package org.project.peerpalapi.dto.app.responses;

import lombok.Builder;

@Builder
public record UserSideBar(
        // profile picture
        // pinned notes for today?
        // upcoming sessions
        // study streak
        // active users and groups
        // task peeks
) {
}
